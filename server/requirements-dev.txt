# Dev tooling for the server, wired in .github/workflows/ci.yml.
# Configuration for all of it lives in pyproject.toml.
# Floors rather than hashes: this set never gets installed on the desktop host,
# only in CI and in a developer's venv.
ruff>=0.16.3
mypy>=2.3.1
pytest>=9.1.1
coverage>=7.15.4
# TestClient (starlette) drives the app through httpx.
httpx>=0.28.1
